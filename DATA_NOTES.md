# DATA_NOTES - 数据说明

> 更新时间：2026-08-17。库文件：`data/mining.db`（SQLite，WAL 模式）。
> 库内实时统计：`.venv/bin/python -m pipeline.run --stats`

## 0. 最终入库规模（2026-08-17 采集）

| 来源 | 类型 | 篇数 | 发布时间范围 |
|---|---|---|---|
| mining.com | news | 250 | 2026-07-14 ~ 2026-08-16（近 35 天，满足 ≥200） |
| gov.cn | policy | 82 | 2008 ~ 2026（政策文件长效，见 §4 时效说明） |
| smm | policy | 34 | 2026-08-17（列表页未带发布时间，time_certain=0） |
| price_feed | price | 320 日报 + 320 行情行 | 2026-05-19 ~ 2026-08-17（64 个交易日 × 5 品种，满足 ≥200） |
| **合计** | | **686 篇 / 4387 向量块** | |

## 1. 数据源与口径

| 源 | source_name | 类型 | 实际采集方式 | 口径说明 |
|---|---|---|---|---|
| 矿业新闻 | `mining.com` | news | RSS 分页（约 15 页）+ 逐条原文全文抓取，trafilatura 抽正文 | 原题建议源，RSS+全文爬取 |
| 政策-国务院 | `gov.cn` | policy | 政策文件库检索接口，14 关键词轮询（稀土/矿产/锂/铜/镍/铁矿石/钨/钼…），标题匹配 | 补充源：权威政策全文 |
| 政策-澳洲 DISR | `disr` | policy | 适配器已实现（列表+详情解析） | **当前部署网络对 industry.gov.au 不可达，运行时自动跳过并记录 ingest_log**；换网络环境即可采集 |
| 资讯-SMM | `smm` | policy | 上海有色网资讯首页文章列表 + 详情页 | 补充源：行业政策动态 |
| 价格-新浪 | `price_feed` | price | 新浪财经公开期货日K线接口 | **替代口径**，见下 |

### 关于"中国稀土集团官网"的说明（重要）

原题建议源之一。实施时发现其常见域名 `chinare.com.cn` **实为中国再保险集团（China
Re）官网**，与稀土无关；中国稀土集团的真实官网域名在当前网络环境下无法确认
（多个候选域名均不可达，搜索引擎亦被反爬拦截）。为避免错误数据污染语料，
该子源未纳入采集（曾误采的 20 篇保险类文章已从库中删除，ingest_log 保留记录）。
政策源由 gov.cn 政策库（含《稀土管理条例》《矿产资源法实施条例》等核心文件）承担。

### 价格数据口径（重要）

原题建议的 LME 官网 / SHFE / 上海钢联均有登录墙或严格频控，本项目统一改用
**新浪财经公开日K线接口**（免登录、含完整历史），口径差异如下：

| symbol | 品种 | 实际口径 | 与原题口径差异 |
|---|---|---|---|
| `LME_CU` | LME铜 | 新浪外盘连续 CAD（美元/吨） | LME 3个月期铜官方收盘价的连续合约代理 |
| `LME_ZN` | LME锌 | 新浪外盘连续 ZSD（美元/吨） | 同上 |
| `LME_NI` | LME镍 | 新浪外盘连续 NID（美元/吨） | 同上 |
| `SHFE_LC` | 碳酸锂 | 广期所主力合约 LC0（元/吨） | 主力连续而非指定月合约 |
| `IRON_ORE` | 铁矿石 | 大商所主力合约 I0（元/吨） | 替代上海钢联现货指数（登录墙内），期货口径 |

价格双写：`prices` 表（结构化，精确数值查询）+ 每品种每交易日一条摘要
Document（`source_type=price`，入向量库供自然语言检索）。

## 2. Schema

### documents（文本：新闻/政策/价格摘要）

| 字段 | 类型 | 说明 |
|---|---|---|
| `doc_id` | TEXT PK | **主键 = sha256(归一化URL) 前 16 位**。价格日报用合成 URL `price://{symbol}/{date}` 保证幂等 |
| `source_type` | TEXT | `news` / `policy` / `price` |
| `source_name` | TEXT | 见上表 |
| `url` / `title` | TEXT | 原文信息 |
| `published_at` | TEXT | ISO 日期。原文缺失时用抓取日兜底且 `time_certain=0` |
| `lang` | TEXT | zh/en（CJK 占比 > 15% 判定） |
| `simhash` | INTEGER | 64 位内容指纹，近重复检测用 |
| `text` | TEXT | 清洗后正文（`标题。正文`，trafilatura 抽取） |
| `ingested_at` | TEXT | 入库时间 |

### chunks（分块 + 向量）

| 字段 | 说明 |
|---|---|
| `chunk_id` PK | `{doc_id}:{idx}` |
| `text` | 正文按 480 字符滑窗分块，overlap 80，短于 60 字符的块丢弃 |
| `content_hash` | chunk 文本 sha1，块级去重 |
| `embedding` | BLOB（float32 × 384）。模型 paraphrase-multilingual-MiniLM-L12-v2（ONNX），L2 归一化后存储，检索为余弦相似度 |

### prices（结构化行情）

主键 `(symbol, trade_date)`；字段 `open/high/low/close/unit/source/change_pct`。
`change_pct` 由前一日收盘计算（最早一天为 NULL）。

### ingest_log

每次采集运行的源/状态/抓取数/入库数，是本文件统计数字的事实来源；单源失败不影响其他源。

## 3. 去重策略（三层）

1. **URL 级（精确）**：`doc_id` 主键。归一化规则：小写 host、去锚点、去
   utm_*/spm/from 等 8 类跟踪参数、排序 query、去尾斜杠。重复运行管道不产生重复。
2. **内容级（近重复）**：正文 SimHash（中文字 2-gram + 英文词，64 位投票），
   海明距离 ≤ 3 判为近重复，保留先入库者。阈值按库内两两距离分布校准
   （150 篇抽样 11175 对：中位数 26，P1 = 9，无关文章最小间距 4），只抓
   原文重发/URL 参数差异级别的重复，不做改写级去重（改写文与无关文章的
   距离区间重叠，强行放宽会误杀）。价格日报不参与（URL 本身已唯一）。
3. **块级**：同一文档内 `content_hash` 相同的块只入库一次（防样板文字重复检索）。

## 4. 时效窗口

- 新闻：发布时间在近 35 天内（30 天 + 时区余量），按发布时间倒序截取 250 条抓全文。
- 政策：gov.cn 收录检索接口返回的全量（政策文件具有长效性，从 2008 年《稀土管理条例》
  前身文件到 2026-05《矿产资源法实施条例》；查询时靠时间过滤保证"近 N 天"语义）；
  SMM 收录列表页可见全部。
- 价格：近 90 个日历天 = 64 个交易日 × 5 品种 = 320 行，日报 Document 同口径。

## 5. 采集行为约束与已知限制

每域名请求间隔 ≥ 2 秒（含随机抖动）；指数退避重试最多 3 次；4xx（403/429 除外）
不重试。不做任何验证码绕过 / 登录态伪造 / 反反爬对抗。robots 与频控导致的失败
如实记录于 ingest_log 并在本文档披露。

已知限制：
1. **DISR 不可达**（当前网络）：澳洲政策类问题（如"澳洲锂出口政策"）库内无直接
   对应文档，检索会返回最接近的澳洲/锂/政策文档；换网络环境重跑 `pipeline.run`
   即可补充。
2. **SMM 发布时间缺失**：列表页不含日期，`time_certain=0`，时间过滤对 SMM 文档
   不生效（默认全量参与）。
3. **中文查询 x 英文文档的召回**依赖查询扩展词表（`serve/retrieve.py` 的 `ZH_EN`，
   约 50 个矿业词），词表外实体（如小公司名）的跨语言召回会退化。
